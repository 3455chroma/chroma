<%= form_with(model: yoyaku, local: true) do |form| %>
  <% if yoyaku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(yoyaku.errors.count, "error") %> prohibited this yoyaku from being saved:</h2>

      <ul>
      <% yoyaku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :名前 %>
    <%= form.text_field :name, id: :yoyaku_name %>
  </div>

  <div class="field">
    <%= form.label :URL %>
    <%= form.text_field :url, id: :yoyaku_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
