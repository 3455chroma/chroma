<%= image_tag('tsuchi.png', class:'tsuchi') %><br>


<% @tsuchi7.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の発売日まで あと３日です</td>
  </div>
<% end %>

<% @tsuchi6.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の発売日まで あと２日です</td>
  </div>
<% end %>

<% @tsuchi5.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の発売日は明日です。店舗に予約した商品は取りに行くことができます。</td>
  </div>
<% end %>

<% @tsuchi4.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の発売日です</td>
  </div>
<% end %>

<% @tsuchi3.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の受け取りができます</td>
  </div>
<% end %>

<% @tsuchi2.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の受け取りができます</td>
  </div>
<% end %>

<% @tsuchi1.each do |tsuchi| %>
  <div>
    <td><%= tsuchi.name %>  の受け取りができます</td>
  </div>
<% end %>

<hr>

<div align="right" style="position: absolute; right: 0; top: 323px;">
<%= link_to image_tag('newtouroku.png', class:'logo'), new_stouroku_path %><br>

<% @cd.each do |cd| %>
  <%= link_to image_tag('cd.png', class:'logo'), stourokus_path(genre_id: cd) %><br>
<% end %>

<% @dvd.each do |dvd| %>
  <%= link_to image_tag('dvd.png', class:'logo'), stourokus_path(genre_id: dvd) %><br>
<% end %>

<% @book.each do |book| %>
  <%= link_to image_tag('hon.png', class:'logo'), stourokus_path(genre_id: book) %><br>
<% end %>

<%= link_to image_tag('memo.png', class:'logo'), mtourokus_path %><br>
</div>